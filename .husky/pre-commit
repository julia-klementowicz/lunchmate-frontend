#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# CHECK IF LINTING PASSES
npm run lint || 
(
    echo "Linting failed, commit aborted"
    exit 1
)

# FORMAT CODE
npm run pcheck || 
(
    echo "Please format your code before committing. Use npm run format."
    exit 1
)